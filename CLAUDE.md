# CLAUDE.md — キャリア診断＆学習プラン提案ツール

このディレクトリで作業する際のガイドです。

## プロジェクト概要

**CAREER COMPASS** — キャリアに悩む求職者・社会人に向けた、AIキャリア診断＆生涯学習プラン提案ツール。
9つの質問に答えると、AIキャリアアナリストが分析したかのような**パーソナライズド学習プラン**を提示する。
さらに無料会員登録（デモ用の疑似認証）をすると、診断結果に連動した**きめ細かい学習ガントチャート＋教材（Coming Soon）＋進捗管理**が使える
「診断＋最適教材で学ぶアプリ」の構成になっている。

- **ターゲット**: キャリアチェンジ・スキルアップ・年収UPに悩む20〜40代
- **用途**: **提案資料・デモ用のプロトタイプ**。営業提案やコンペでそのまま画面を見せられる完成度を目標にしている
- **デザイン方針**: 信頼感・先進性・人生を託せる重厚感。ダークな ink 系背景＋ゴールド／アイリスのアクセント、
  glassmorphism のカード、スムーズなマイクロインタラクション

> ⚠️ **このツールは AI もデータベースも実際には使っていない。** 解析画面の演出、AI所見の文章、
> スコア・年収レンジ・市場指標はすべてフロントエンド内で生成・保持しているサンプル値。
> 「技術スタック」の節にある「本番実装で置き換えるべきもの」を必ず読むこと。

## ファイル構成

| ファイル | 役割 |
| --- | --- |
| `index.html` | **本体（唯一の実行ファイル）**。`<style>`＋HTML＋`<script>` を1ファイルに統合した完全なHTML文書 |
| `favicon.svg` | ファビコン本体（方位環つきのコンパス針）。`<link rel="icon">` から参照 |
| `favicon-small.svg` | **16/32px PNG の生成元**。環を省き針を太くした小サイズ最適化版。HTMLからは参照しない |
| `favicon-16.png` / `favicon-32.png` | `favicon-small.svg` から生成。SVG非対応ブラウザ向けフォールバック |
| `favicon-48.png` | `favicon.svg` から生成。Windows のショートカット等 |
| `favicon-180.png` | `favicon.svg` から生成。`apple-touch-icon` |
| `favicon-512.png` | `favicon.svg` から生成。PWA / OG 用途・サイトアイコン登録用 |

## 画面フロー

`index.html` 内の5つの `<section>` を `show()` で出し入れする（SPA的な単一ページ遷移。URLは変わらない）。
加えて `<body>` 直下に2つのモーダル（`#auth-modal` / `#material-modal`）がある。

| # | セクションID | 内容 |
| --- | --- | --- |
| ① | `view-intro` | ヒーロー。診断開始ボタン |
| ② | `view-quiz` | 9問のマルチステップフォーム。プログレスバー＋ドットインジケータ |
| ③ | `view-loading` | 擬似AI解析演出。回転オーブ＋6段のログが順次 `done` になる |
| ④ | `view-result` | 診断レポート（スコア／AI所見／レーダー／回答サマリー／12ヶ月工程表／学習テーマ／ROI）＋「無料会員登録をする」CTA |
| ⑤ | `view-member` | 会員マイページ。進捗サマリー＋学習スケジュール（詳細ガントチャート）。未診断時は空状態（`#m-empty`）を表示 |

| モーダルID | 内容 |
| --- | --- |
| `auth-modal` | ログイン／無料会員登録のタブ切り替えフォーム（疑似認証） |
| `material-modal` | 教材画面。現時点はコンテンツ枠に「COMING SOON」を表示し、進捗ステータス（未着手/進行中/完了）をここで更新する |

### 設問（9問）

| # | `id` | type | 使われ方 |
| --- | --- | --- | --- |
| Q1 | `industry` | single | `INDUSTRY` を引き、6軸の初期値・強みの文章・年収のフォールバック値を決める |
| Q2 | `salary` | single | `SALARY` を引き、**ROI試算の起点**と市場価値スコアの補正に使う |
| Q3 | `strengths` | multi | `STRENGTH_AXIS` 経由でレーダーを補正し、AI所見の強み段落に差し込む |
| Q4 | `issues` | multi | AI所見の冒頭とリスク段落の分岐 |
| Q5 | `values` | multi | `DIRECTION_FIT` と突き合わせて**適合度スコア**を出す |
| Q6 | `direction` | single | **アーキタイプ判定の中心**。`ARCHETYPES` のキーになる |
| Q7 | `constraints` | multi | 方向性と衝突するNG条件の検出。`exclusive:'none'` を持つ唯一の設問 |
| Q8 | `hours` | single | `PACE` による工程の伸縮＋学習投下量スコアの補正 |
| Q9 | `budget` | single | AI所見の前提文 |

ヘッダーのロゴ（`#btn-home`）と結果画面の「もう一度診断する」（`#btn-retry`）は、
どちらも `resetToIntro()` で**回答を破棄して①へ戻る**。

## 技術スタック

### 現状（デモ実装）

| 領域 | 現状 |
| --- | --- |
| 構成 | **単一HTMLファイル**。ビルドステップなし。`index.html` をブラウザで開けば動く |
| CSS | **Tailwind CSS Play CDN**（`cdn.tailwindcss.com`）＋ `<style>` 内のカスタムCSS。`tailwind.config` はインラインで上書き |
| フォント | Google Fonts（Noto Serif JP / Noto Sans JP / Outfit） |
| JS | **素の Vanilla JS**。フレームワーク・ライブラリ・ビルドツールすべてなし。全体を1つのIIFEで囲みグローバル汚染を回避 |
| チャート | **手書きSVG**（レーダー）＋ **CSS Grid**（ガントチャート）。チャートライブラリ不使用 |
| AI解析 | **擬似**。`setTimeout` でログを順送りする演出のみ。API呼び出しは一切ない |
| 文章生成 | **テンプレート合成**。`buildAnalysisText()` が回答値を差し込んで5段落を組み立てる |
| データ | **JSオブジェクトのハードコード**（`QUESTIONS` / `INDUSTRY` / `SALARY` / `ARCHETYPES` / `DIRECTION_FIT` / `STRENGTH_AXIS` / `TONE` / `PACE`） |
| 認証 | **疑似（なんちゃって）認証**。ユーザー情報を localStorage に保存するだけで、サーバー・ハッシュ化なし。**パスワードは平文保存**（デモ限定） |
| 永続化 | **localStorage のみ**。会員情報・診断結果・学習進捗を保存する（下記「会員機能」の節を参照）。診断フォームの回答自体はリロードで消える |
| 計測 | **なし** |

**外部依存は Tailwind CDN と Google Fonts の2つだけ。** どちらもオフラインでは失敗し、
Tailwind が落ちると**スタイルが完全に外れる**（クラス名しか書いていないため）。
提案の場でネットワークが不安な場合は、事前にビルド版CSSへ差し替えておくこと。

### 本番実装で置き換えるべきもの

| 領域 | 現状 | 本来やるべき実装 |
| --- | --- | --- |
| CSS | Tailwind Play CDN | **ビルド導入**（Vite / PostCSS）でパージ済みCSSを生成。Play CDN は本番非推奨で、初回描画も遅い |
| フォント | Google Fonts CDN | セルフホスト＋`font-display:swap`＋サブセット化（日本語フォントは容量が大きい） |
| 画面遷移 | `show()` でDOM出し入れ | ルーティング（結果ページにURLを持たせる）。現状は**結果をブックマーク・共有できない** |
| AI解析 | 擬似演出＋テンプレート文章 | **Claude API を実呼び出し**（下記の詳細を参照） |
| マスタデータ | JSオブジェクト | DB または ヘッドレスCMS。設問・アーキタイプ・市場データを**非エンジニアが更新できる**状態にする |
| 市場指標・年収 | ハードコードのサンプル値 | 求人統計・賃金統計の実データ連携。**出典と取得日を画面に明記**する |
| 診断結果 | localStorage（会員のみ） | DBへ保存し、結果URLの発行・再閲覧・メール送付を可能にする |
| 認証 | localStorage の疑似認証（平文パスワード） | 本物の認証基盤（Auth0 / Supabase Auth / Firebase Auth 等）。パスワードはサーバー側でハッシュ化し、セッションは httpOnly Cookie で管理する。**現状の実装は絶対にそのまま公開しない** |
| 学習進捗 | localStorage（端末ローカル） | DBへ保存し、複数端末間で同期する |
| 入力バリデーション | クライアントのみ | サーバー側でも検証（クライアント側は改変可能） |
| 計測 | なし | GA4 等で離脱ステップ・完了率・方向性の選択分布を計測。**どの設問で落ちるか**が改善の起点になる |
| テスト | なし | 診断ロジックのユニットテスト＋主要フローのE2E（Playwright） |
| アクセシビリティ | `aria-*` / `:focus-visible` / `prefers-reduced-motion` は対応済み | スクリーンリーダー実機検証、コントラスト比の全面監査 |

#### Claude API を実装する場合の要点

**現在のデータ構造が、そのままAPIレスポンスのスキーマになる設計にしてある。**
`ARCHETYPES` の1エントリ（`title` / `tagline` / `badges` / `roadmap` / `tracks` / `themes` / `roi` / `metrics`）を
JSON Schema として定義し、`renderGantt()` / `renderThemes()` の描画コードは変更せずに中身だけ実データへ差し替えられる。

- **APIキーは絶対にクライアントへ置かない。** 必ず自前のサーバー（またはEdge Functions）を経由させる。
  ブラウザから直接 Anthropic API を叩く実装は、キーが公開されるため不可
- **モデル**: `claude-opus-5`。思考はデフォルトで有効なので `thinking` は省略でよい。
  深さ・コストは `output_config: { effort: "low" | "medium" | "high" | "xhigh" | "max" }` で調整する
- **構造化出力**: `output_config.format` に `json_schema` を渡し、上記スキーマに沿ったJSONを直接受け取る。
  プロンプトで「JSONで返して」と指示して文字列をパースする実装にはしない
- **プロンプトキャッシュ**: 設問定義や評価ルールなど固定部分は前方に置き `cache_control` を付ける。
  変動する回答値は必ずキャッシュ境界より**後ろ**に置く（前に入れるとキャッシュが毎回無効化される）
- **拒否応答のハンドリング**: `stop_reason === "refusal"` を `content` の読み取り前に判定する。
  `content[0]` を無条件に参照する実装は落ちる。サーバー側フォールバックの併用を推奨
- **プロンプトインジェクション対策**: 自由記述の設問を追加する場合、ユーザー入力を指示として解釈させない構造にし、
  生成結果はスキーマ検証を通してから描画する
- **レート制限とコスト管理**: 診断1回ごとにAPI課金が発生する。IP/セッション単位のレート制限と、
  同一回答パターンに対する結果キャッシュを入れる

## アーキテクチャと重要な設計判断

### 状態管理
`answers`（回答オブジェクト）と `step`（現在の設問インデックス）の2つだけ。
`answers` は設問IDをキーに持ち、単一選択は文字列・複数選択は配列を格納する。

### 診断ロジック
- **アーキタイプ判定**: Q6「目指したい方向性」の値がそのまま `ARCHETYPES` のキーになる（10種）。
  ここが分岐の中心で、ロードマップ・工程表・学習テーマ・ROI・市場指標のすべてが方向性ごとに固有
- **スコア算出**: `computeScores()` が `INDUSTRY[業界].base`（6軸の初期値）に補正を掛け、
  `ARCHETYPES[方向性].gain`（プランによる伸び）を加算する。補正は3系統：
  - 年収帯（`SALARY[].band`）が上位2帯なら市場価値 +1、下位2帯なら −1
  - 得意領域（Q3）を `STRENGTH_AXIS` で軸ごとに集計し、**同じ軸に2つ以上集まった軸だけ** +1。
    広く浅い自己申告で全軸が上がるのを防ぐための閾値であり、1つ選んだだけでは加点しない
  - 週15h以上なら学習投下量 +1、週3h未満なら −1
- **適合度スコア**: `computeAlignment()` が Q5の価値観と `DIRECTION_FIT[方向性].values` の一致率から
  45〜95% を出し、`DIRECTION_FIT[方向性].clash` に該当するNG条件（Q7）1件ごとに −12% する。
  結果は「INPUT PROFILE」カードのメーター・AI所見の第3段落・⚠ 付きチップの3箇所に出る
- **ROI の起点**: Q2の自己申告年収（`SALARY[].range`）を使う。
  `none`（未回答）を選んだ場合のみ `INDUSTRY[業界].salary` へフォールバックし、
  ラベルも「現在の想定レンジ（業界平均）」に切り替える
- **ペース補正**: Q8の確保時間に応じて `PACE` から文章を出し、12ヶ月の工程を
  16〜18ヶ月／9〜12ヶ月／6〜8ヶ月へ伸縮する前提を明示する
- **リスク段落の優先順位**: 方向性とNG条件の衝突 → `やりたいことが分からない` → 週3h未満 →
  `1年以上かかる学習`がNG → `労働時間`/`家庭と両立` → `将来性`/`会社の先行き` →
  デフォルト（インプット偏重）の順で**最初に該当した1つだけ**を出す

### アニメーションは必ず「最終状態を書き込む保険」を付ける（重要）
スコアのカウントアップとレーダーの描画は `tween()` を経由している。
**`requestAnimationFrame` は非表示タブでは発火しない**ため、rAFだけに依存すると
結果画面がバックグラウンドで描画された場合にスコアが 0 のまま固定される。
`tween()` は `setTimeout(finish, dur + 200)` で最終値を必ず書き込み、
`prefers-reduced-motion` 指定時はアニメーションを飛ばして即座に最終状態にする。
**新しいアニメーションを追加する場合も同じ構造にすること。**
ガントチャートのバーはCSSトランジション（`.g-bar.on`）なので、この問題は起きない。

### ガントチャートのCSS Grid実装
`.g-row` は `grid-template-columns: 12rem repeat(12, minmax(0,1fr))` で、
1列目がテーマ名、2〜13列目が1〜12ヶ月。**グリッド線用のセル・フェーズ帯・バーをすべて `grid-row:1` に重ねて配置**し、
バーは `grid-column: start+2 / span len` で期間を表現している（`z-index` で前後を制御）。
横幅は `min-w-[780px]` を `overflow-x-auto` で包み、**ページ全体には横スクロールを出さない**。

### 会員機能（疑似認証・マイページ・進捗管理）

`index.html` のスクリプト「13. 会員機能」にまとまっている。すべてフロントエンド完結のデモ実装。

**localStorage のキー構成:**

| キー | 内容 |
| --- | --- |
| `cc_users` | `{ [email]: { name, pass } }`。**パスワードは平文**（デモ限定。UIにも注記あり） |
| `cc_session` | ログイン中の email。これが在ればページ再訪時もログイン状態で始まる |
| `cc_diag::<email>` | `{ answers, savedAt }`。診断完了時（ログイン済み）または登録/ログイン成功時に、直近の診断結果 `latestDiagnosis` を保存する |
| `cc_progress::<email>` | `{ [taskId]: 'todo' \| 'doing' \| 'done' }`。教材モーダルからの進捗更新で書き換える |

**学習スケジュール（詳細ガントチャート）の生成:** `buildMemberTasks(dirKey)` が `ARCHETYPES[方向性].tracks`（9本）を
`SUBSTEPS[tone]`（工程種別ごとのサブステップ。各要素は `{ name, goal }` で、name は成果物ベースの行動、
goal は到達目標。core / practice / output / transition は4段、habit は3段）で最大4分割し、約25〜30個のタスクに展開する。
分割数は `min(サブステップ数, トラックの月数)`、期間は月数を等分して端数を前に寄せる。
バーの中には `goal`（到達目標）を表示し、`title` 属性でツールチップも出す。
さらに `relatedTheme()` がトラック名と `themes` の2-gram一致度でもっとも関連の深い学習テーマを引き当て
（一致度2未満なら null）、教材モーダルに「関連する学習テーマ」として `items` を表示する。
タスクIDは `"<方向性>:<トラック番号>:<サブステップ番号>"` 形式なので、**進捗は方向性ごとに独立して保持**され、
再診断して方向性が変わっても以前の進捗は消えない。

**描画:** マイページのガントは結果画面と同じ CSS Grid 構成（`.g-row` / `.g-cell` / `.g-bar`）を再利用し、
バーだけ `<button class="g-bar g-task on">` にしてクリックで教材モーダルを開く（イベント委譲）。
`on` を最初から付けて描画するため、進捗更新による再描画でもバーの伸びる演出は再生されない（意図的）。
状態は `data-status` 属性で表現し、CSSで 完了=減光 / 進行中=ミントの輪郭 を当てる。

**テストユーザーと見本プラン:** 起動時に `seedTestUser()` が
`test@example.com / test1234`（名前：テストユーザー）を `cc_users` にシードし、
診断結果として `direction:'ai_driven_dev'` を**毎回上書き**保存する。認証モーダルにはこの資格情報を
オレンジ文字で常時案内している。`ai_driven_dev` は **ARCHETYPES 内にあるがQ6の選択肢ではない**
マイページ専用の見本プラン（AI駆動開発）で、`title` / `tracks` / `themes` しか持たない。
診断フローから参照されることはないため `DIRECTION_FIT` や `roadmap` / `roi` / `metrics` は不要。
**Q6に追加してはいけない**（追加するなら通常アーキタイプの必須プロパティを全部揃えること）。
テストユーザーは見本プラン固定にするため、`buildResult()` と `onAuthSuccess()` の両方で
診断結果の自動保存から除外している。

**モーダル:** `openModal()` / `closeModal()` を `#auth-modal` と `#material-modal` で共用。
`hidden` 属性で開閉し（`.modal[hidden]{display:none}` が必要）、背景・✕（`data-close`）・Esc で閉じる。
ユーザー名など**ユーザー入力を画面に出すときは必ず `textContent` を使う**（`innerHTML` に混ぜない。XSS防止）。

### CSS詳細度の注意
Tailwind のユーティリティとカスタムCSSが混在している。`.g-row` / `.opt` / `.orb` などの
コンポーネントクラスは `<style>` 内に定義し、レイアウト・色はTailwindクラスで当てている。
`.opt[aria-pressed="true"]` のように**状態は属性セレクタで表現**しており、JSはクラスではなく
`aria-pressed` を更新する（アクセシビリティと見た目を同時に満たすため）。

## データを変更するとき

### 設問を追加・変更する
1. `QUESTIONS` 配列にエントリを追加（`id` / `type` / `cat` / `title` / `hint` / `options`）
2. `type: 'multi'` なら複数選択、`'single'` なら単一選択。**単一選択でも自動遷移はしない**（誤タップ防止のため意図的）
3. 表示用のラベルマップ（`ISSUE_TEXT` / `VALUE_TEXT` / `STRENGTH_TEXT` / `NG_TEXT` / `HOURS_TEXT` /
   `BUDGET_TEXT`、年収は `SALARY[].label`）も同期する。ここが漏れると AI所見と回答サマリーに `undefined` が出る
4. 選択肢が **9個以上**の設問は自動的に3カラム表示に切り替わる（`renderStep()` 内の `many` 判定）。
   3カラムでは `desc` が**12文字を超えると2行に折り返す**ため、カードの高さを揃えたい場合は12文字以内に収める
5. `exclusive: '<選択肢のv>'` を設問に付けると、その選択肢は他と両立しない排他選択になる
   （「特に制約はない」用。`select()` が全ボタンの `aria-pressed` を貼り直す）
6. 新しい設問を結果画面の「INPUT PROFILE」に出す場合は `renderProfile()` の `rows` にも1行足す
7. `#q-total` は `QUESTIONS.length` から自動反映されるが、**イントロ本文とmeta descriptionの
   「9つの質問」は手書き**なので設問数を変えたら直す

### 方向性（アーキタイプ）を追加する
Q6の選択肢を1つ増やす場合、`ARCHETYPES` に**同じキーで**次のすべてを揃える必要がある。
どれかが欠けると結果画面が崩れる。加えて **`DIRECTION_FIT` にも同じキーを追加する**
（`values`＝相性のよい価値観、`clash`＝衝突するNG条件）。ここが漏れると適合度が
`ai_dx` の値で計算され、他人のプロフィールに対する判定が出る。

| キー | 内容 |
| --- | --- |
| `title` / `tagline` / `badges` | 見出し・説明文・バッジ3つ |
| `demand` | 市場需要インデックス（0〜100） |
| `gain` | プラン完了後の6軸の伸び（レーダーの差分） |
| `roadmap` | 3フェーズ × `{ term, head, body, kpis[3] }` |
| `roi` | `{ y1, y3, payback }`。年収レンジの倍率と回収目安 |
| `metrics` | 市場ポジション指標4件 × `{ label, value, bar, color }` |
| `tracks` | 工程表のトラック9本 × `{ name, start, len, tone, note }`（`start`は0起点の月、`len`は月数） |
| `themes` | 学習テーマ4件 × `{ title, why, items[4], hours, goal }` |

`tone` は `TONE` のキー（`core` / `practice` / `output` / `transition` / `habit`）から選ぶ。
凡例は実際に使われている `tone` だけを自動抽出して表示する。

## 注意事項

- **疑似認証であることの注記を削除しない。** 認証モーダル下部の
  「デモ用の疑似認証です。…実在のパスワード等は入力しないでください」は、平文保存の実装を本物と誤認させないための注記。
  本物の認証基盤に置き換えるまで消さないこと
- **サンプル値であることの明示を削除しない。** スコア・年収レンジ・市場指標はすべて架空の値であり、
  イントロ・ROIセクション・フッターの3箇所に注記を置いている。実データに差し替えるまで消さないこと。
  実在サービスの紹介や具体的な収入の保証と読める表現を追加すると、景品表示法上の問題になり得る
- **特定サービスへの誘導は行わない設計**。かつて架空の学習サービスをカード表示していたが、
  「何を学ぶべきか」を伝える構成へ意図的に変更した。サービス名・価格・マッチ度を再導入しないこと
- **「学習スタイル」設問（動画／マンツーマン／ゼミ 等）を復活させないこと。**
  これは学習サービスを推薦するための設問であり、「理想を実現するために何を学ぶべきか」を示す
  本ツールの目的から外れるため削除した。代わりに年収・価値観・得意領域・NG条件を尋ね、
  **回答の内訳を診断の材料として使う**方向で設問を増やしている
- **画面に出しているモデル名は `CLAUDE OPUS 5`**（ローディングのログと結果ヘッダーの2箇所）。
  演出用の文字列なので、実装と乖離させたくない場合はここも合わせて更新する
- 実際のAI・DBを繋いだ場合は、上記「本番実装で置き換えるべきもの」の注記も併せて見直すこと

## ローカルでの表示確認手順

`index.html` は完全なHTML文書なので、ブラウザで直接開けば確認できる。
ヘッドレスChromeでのスクリーンショット検証も可能（レイアウト崩れの確認に有効）。

```bash
# 通常のキャプチャ（アニメーション完了を待つため virtual-time-budget を付ける）
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --disable-gpu \
  --window-size=1280,1200 --virtual-time-budget=8000 --screenshot=out.png \
  "file:///path/to/career-diagnosis/index.html"
```

**ヘッドレス検証時の既知の落とし穴:**

- **`--virtual-time-budget` 下では `performance.now()` と rAF のタイムスタンプが同期しない。**
  カウントアップやレーダーが 0 のまま撮れることがあるが、これは**実ブラウザでは起きない環境固有の挙動**。
  数値を確認したい場合は **`--force-prefers-reduced-motion`** を付けると最終状態が同期的に書き込まれる
- **モバイル幅は `--window-size=390,...` では再現できない**（clientWidth が 490px 程度に丸められる）。
  目的幅の `<iframe>` を持つラッパーHTML経由で撮ると実機に近い
- 結果画面は縦が長いため、`view-result` の子要素を `display:none` で間引いてから撮ると目的のセクションを拡大確認できる

## ファビコンの再生成手順

PNG は SVG からヘッドレスChromeで書き出している（`--default-background-color=00000000` で角丸の外side を透過させる）。

```bash
# 48 / 180 / 512 は favicon.svg から、16 / 32 は favicon-small.svg から生成する
# SVG を size px 指定でインラインしたラッパーHTMLを作り、同サイズの window で撮影する
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --disable-gpu \
  --hide-scrollbars --default-background-color=00000000 \
  --window-size=512,512 --virtual-time-budget=1500 --screenshot=favicon-512.png \
  "file:///path/to/wrapper.html"
```

**16/32px を `favicon.svg` から作らないこと。** 方位環が潰れて視認性が落ちるため、
環を省いて針を拡大した `favicon-small.svg` を小サイズ専用の生成元にしている。
